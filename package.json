{
  "name": "empleados-api-backend",
  "version": "2.0.0",
  "description": "API para gestión de empleados con Base de Datos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "init-db": "node database/init.js",
    "reset-db": "rm -f database/empleados.db && node database/init.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "better-sqlite3": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}